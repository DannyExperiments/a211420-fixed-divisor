name: Lean verification

on:
  push:
    branches: [main]
    paths:
      - "*.lean"
      - "**/*.lean"
      - "lean-toolchain"
      - "lake-manifest.json"
      - "scripts/verify.sh"
      - ".github/workflows/lean.yml"
  pull_request:
    branches: [main]
    paths:
      - "*.lean"
      - "**/*.lean"
      - "lean-toolchain"
      - "lake-manifest.json"
      - "scripts/verify.sh"
      - ".github/workflows/lean.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and replay Lean declarations through the kernel
        uses: leanprover/lean-action@v1
        with:
          build: true
          leanchecker: true
          use-mathlib-cache: true
      - name: Reject forbidden project proof escapes
        run: ./scripts/verify.sh
